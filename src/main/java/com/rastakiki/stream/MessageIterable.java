package com.rastakiki.stream;

import com.google.protobuf.GeneratedMessage;

import java.util.Iterator;

public class MessageIterable<T extends GeneratedMessage> implements Iterable<T> {

    @Override
    public Iterator<T> iterator() {
        return null;
    }
}
